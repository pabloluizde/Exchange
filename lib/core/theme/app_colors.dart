import 'package:flutter/material.dart';

class AppColors {
  static const defaultRed = Color.fromRGBO(226, 36, 42, 1.0);
  static const defaultWhite = Colors.white;
  static const defaultLightGrey = Color.fromRGBO(196, 196, 196, 0.58);

  static const defaultBlack = Colors.black;
  static const defaultGrey = Color.fromRGBO(32, 33, 33, 1);
  static const defaultGreyModal = Color.fromRGBO(30, 30, 30, 1);
  static const defaultGold = Color.fromRGBO(218, 165, 32, 0.72);
}
